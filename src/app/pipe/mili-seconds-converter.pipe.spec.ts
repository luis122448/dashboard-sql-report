import { MiliSecondsConverterPipe } from './mili-seconds-converter.pipe';

describe('MiliSecondsConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MiliSecondsConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
