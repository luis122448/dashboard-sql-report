import { SecondsConverterPipe } from './seconds-converter.pipe';

describe('SecondsConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
